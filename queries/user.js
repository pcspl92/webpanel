const query = require('../utils/queryTemplate');

const viewUsers = () => {
  const sql = ``;
  return query(sql);
};

const createUsers1=()=>{

  const sql=`INSERT INTO `;
}

module.exports = { viewUsers };
